# Core dependencies for AugmentAI
# For full installation, use: pip install -e ".[all]"
# For development, use: pip install -e ".[dev]"

pydantic>=2.0
pyyaml>=6.0
rich>=13.0
typer>=0.9
openai>=1.0
httpx>=0.25
albumentations>=1.3
numpy>=1.24
pillow>=10.0
python-dotenv>=1.0
